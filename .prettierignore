# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Standalone demo-recording harness — its own mini-project (separate
# package.json / node_modules), not subject to the app's formatting rules.
/demo/
